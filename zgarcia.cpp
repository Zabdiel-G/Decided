//
//Author: Zabdiel Garcia
//
//Source code: zgarcia.cpp
//

#include <iostream>
using namespace std;

void messageZ()
{
    cout << "Zabs Feature mode has been Entered" << endl;
}

/*
void obstRend()
{
    glClear(GL_COLOR_BUFFER_BIT);
    //Draw Box
    for (int i = 0; i < 5; ++i){
        glPushMatrix();
        glColor3ub(100, 200, 100);
        glTranslatef(obst[i].pos[0], obst[i].pos[1], 0.0f);
        glBegin(GL_QUADS);
        glVertex2f(-obst[i].w, -obst[i].h);
        glVertex2f(-obst[i].w, obst[i].h);
        glVertex2f( obst[i].w, obst[i].h);
        glVertex2f( obst[i].w, -obst[i].h);
        glEnd();
        glPopMatrix();
    }
}
*/

